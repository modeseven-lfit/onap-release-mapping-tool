---
# SPDX-License-Identifier: Apache-2.0
# SPDX-FileCopyrightText: 2025 The Linux Foundation

# Action test/validation workflow
name: "Test GitHub Action"

# yamllint disable-line rule:truthy
on:
  workflow_dispatch:
  pull_request:
    branches: ["main"]

concurrency:
  group: "${{ github.workflow }}-${{ github.ref }}"
  cancel-in-progress: true

permissions: {}

jobs:
  tests:
    name: "Test local GitHub Action"
    runs-on: ubuntu-latest
    permissions:
      contents: read
    timeout-minutes: 10
    steps:
      # yamllint disable-line rule:line-length
      - uses: step-security/harden-runner@ab7a9404c0f3da075243ca237b5fac12c98deaa5 # v2.19.3
        with:
          egress-policy: audit

      - name: "Checkout repository"
        # yamllint disable-line rule:line-length
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0

      - name: "Create minimal OOM structure"
        shell: bash
        run: |
          # Create minimal OOM structure for testing
          mkdir -p oom/kubernetes/onap
          cat > oom/kubernetes/onap/Chart.yaml << 'EOF'
          apiVersion: v2
          name: onap
          version: 0.0.0-test
          description: Test chart
          dependencies: []
          EOF

      - name: "Run local action"
        uses: ./
        with:
          oom-path: "./oom"
          release-name: "Test"
          output-dir: "./output"

      - name: "Verify manifest was created"
        shell: bash
        run: |
          # Verify manifest was created
          if [ -f "./output/manifest.json" ]; then
            echo "Manifest created successfully"
            cat ./output/manifest.json | python3 -m json.tool
          else
            echo "Error: manifest.json not found"
            exit 1
          fi
